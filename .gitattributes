*.ipynb filter=nbstripout
*.zpln filter=nbstripout
*.ipynb diff=ipynb

# The original Colab notebooks are a historical reference: their outputs are the
# evidence of the reported results (loss curves, perplexity, sample generations).
# Keep them byte-for-byte as pulled from Drive — do not strip.
notebooks/original/**.ipynb -filter -diff
